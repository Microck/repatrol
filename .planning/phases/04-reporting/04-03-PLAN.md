---
phase: 04-reporting
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/agents/reporter_agent.py
  - src/agents/__init__.py
  - scripts/run_reporter.py
autonomous: true

must_haves:
  truths:
    - "Reporter can synthesize a human-readable bug report from BugReport artifacts"
    - "Reporter can create a GitHub issue (or dry-run) for a captured bug"
  artifacts:
    - path: "src/agents/reporter_agent.py"
      provides: "Reporter agent: synthesize -> publish evidence -> create issue"
      contains: "class ReporterAgent"
    - path: "scripts/run_reporter.py"
      provides: "Run Reporter against a saved BugReport JSON"
      contains: "--dry-run"
  key_links:
    - from: "src/agents/reporter_agent.py"
      to: "src/integrations/github.py"
      via: "GitHubIssueClient.create_issue"
      pattern: "create_issue"
---

<objective>
Implement the Reporter agent that turns raw bug artifacts into a polished issue and files it (or dry-runs) automatically.

Purpose: Make the demo visceral: bug -> issue with evidence.
Output: Reporter agent + CLI.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/models/bug.py
@src/storage/evidence.py
@src/integrations/github.py
@src/llm/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ReporterAgent with deterministic issue formatting</name>
  <files>
src/agents/__init__.py
src/agents/reporter_agent.py
  </files>
  <action>
Implement reporter behavior with a stable output template.

- Inputs: `BugReport`, `EvidenceStore`, `GitHubIssueClient`, optional `LLMClient`.
- Behavior:
  - publish evidence refs (local or url) for both screenshot and video when present
  - generate title (detector + short summary)
  - generate issue body with sections required by REP-01
  - optionally use `LLMClient.text()` to rewrite summary into a clearer paragraph (but keep a deterministic fallback if no creds).
- Ensure bug report always includes repro steps; if missing, synthesize from stored action log placeholders.
- Ensure the issue body includes a dedicated "Evidence" section with:
  - screenshot link/path
  - video link/path (from `BugReport.evidence.video_path` / `EvidenceRef.url` when available).
  </action>
  <verify>
python3 -m compileall src
python3 -c "from src.agents.reporter_agent import ReporterAgent; print(ReporterAgent)"
  </verify>
  <done>
ReporterAgent can render a complete issue markdown body from a BugReport without needing network access.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Reporter CLI runner for saved bug artifacts</name>
  <files>
scripts/run_reporter.py
  </files>
  <action>
Create `scripts/run_reporter.py` that:

- accepts `--bug <path-to-bugreport.json>` (e.g., `artifacts/bug_smoke.json`)
- supports `--dry-run` and prints rendered body
- in real mode, creates the issue and prints the URL.
  </action>
  <verify>
python3 scripts/run_reporter.py --help
python3 scripts/bug_model_smoke.py --out artifacts/bug_smoke.json
python3 scripts/run_reporter.py --dry-run --bug artifacts/bug_smoke.json
  </verify>
  <done>
Reporter can be run standalone and produces stable output suitable for demo screenshots.
  </done>
</task>

</tasks>

<verification>
- Running `scripts/run_reporter.py` on a captured bug writes/prints a complete issue template.
</verification>

<success_criteria>
- REP-01 unblocked: findings synthesized into a report. REP-03 wired via GitHub client.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reporting/04-03-SUMMARY.md`
</output>
