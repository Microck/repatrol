---
phase: 05-demo-submit
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - README.md
  - demo/demo_script.md
  - demo/demo_checklist.md
autonomous: true

must_haves:
  truths:
    - "README tells a new user how to run the demo end-to-end"
    - "There is a 2-minute demo script/checklist aligned to the orchestrator output"
  artifacts:
    - path: "README.md"
      provides: "Project overview + setup + demo commands"
      contains: "Repatrol"
    - path: "demo/demo_script.md"
      provides: "Narration script / shot list for a 2-minute demo"
      contains: "2-minute"
  key_links:
    - from: "README.md"
      to: "scripts/run_demo.py"
      via: "documented command"
      pattern: "run_demo.py"
---

<objective>
Polish documentation and demo assets so the project is runnable and presentable in under 2 minutes.

Purpose: Judges need a clean path to value.
Output: README + demo script/checklist.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@../../plans/stream-qa-swarm.md
@scripts/run_demo.py
@scripts/run_swarm.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README focused on demo-first onboarding</name>
  <files>
README.md
  </files>
  <action>
Create a README that optimizes for "run the demo" first.

- Sections:
  - What it is (1 paragraph)
  - Demo quickstart (the exact commands; include `python3 scripts/run_demo.py ...`)
  - How it works (Explorer/Chaos/Reporter in 5 bullets)
  - Configuration (Foundry optional; GitHub token for issue filing)
  - Artifacts layout (`artifacts/`)
- Keep it short, accurate, and aligned to actual scripts.
  </action>
  <verify>
python3 -c "import pathlib; print(pathlib.Path('README.md').exists())"
  </verify>
  <done>
README exists and includes a demo quickstart command that matches `scripts/run_demo.py`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a 2-minute demo script + execution checklist</name>
  <files>
demo/demo_script.md
demo/demo_checklist.md
  </files>
  <action>
Add lightweight demo materials.

- `demo/demo_script.md`:
  - 2-minute narration flow aligned to the intended demo scenario
  - show: Explorer coverage summary -> Chaos bug -> Reporter issue creation
- `demo/demo_checklist.md`:
  - pre-demo checks (deps installed, token set, server port free)
  - "recording" checklist (start, run, verify artifacts, rerun if needed)
  - a fallback plan if GitHub token is unavailable (dry-run mode).
  </action>
  <verify>
python3 -c "import pathlib; print(pathlib.Path('demo/demo_script.md').exists())"
  </verify>
  <done>
Demo script and checklist exist and match the actual CLI output fields.
  </done>
</task>

</tasks>

<verification>
- README and demo script are consistent with the real CLI flags and artifact paths.
</verification>

<success_criteria>
 - DEMO-04 unblocked: README + demo script/checklist are complete and reference the real demo artifacts (including `demo/video.mp4`).
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-submit/05-03-SUMMARY.md`
</output>
